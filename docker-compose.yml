# # version: '2'
# services:
#   php:
#     image: yiisoftware/yii2-php:7.4-apache
#     volumes:
#       - ~/.composer-docker/cache:/root/.composer/cache:delegated
#       - ./:/app:delegated
#     ports:
#       - '8000:80'

# version: "3.9"
services:
  php:
    build: .                 # build dari Dockerfile di folder ini
    container_name: yii2_app
    volumes:
      - ./:/var/www/html     # mount source code
    ports:
      - "9090:90"            # host:container (port 90 di container)
